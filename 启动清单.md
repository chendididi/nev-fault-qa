# NEV Fault QA 启动清单

适用目录：`/root/ubuntuchen_file/nev-fault-qa`

## 0. 远程不稳定时先用 tmux + Codex

新开或附着到固定会话：

```bash
cd /root/ubuntuchen_file/nev-fault-qa
./scripts/start_codex_tmux.sh
```

如果 Codex 进程已经退出，但想恢复上一次聊天上下文：  

```bash
cd /root/ubuntuchen_file/nev-fault-qa
./scripts/start_codex_tmux.sh resume
```

等价的 Make 入口：

```bash
make codex-tmux
make codex-resume
```

如果只是 SSH 断开，重连后通常只需要：

```bash
tmux attach -t nev
```

## 1. 进入项目目录

```bash
cd /root/ubuntuchen_file/nev-fault-qa
```

## 2. 启动基础服务

```bash
docker compose up -d
```

检查容器状态：

```bash
docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
```

## 3. 启动后端 API

```bash
mkdir -p logs
nohup ../.venv/bin/uvicorn src.api.main:app --host 0.0.0.0 --port 8000 > logs/uvicorn.out 2>&1 &
```

## 4. 启动前端 Streamlit

```bash
nohup ../.venv/bin/streamlit run src/frontend/streamlit_app.py --server.address 0.0.0.0 --server.port 8501 > logs/streamlit.out 2>&1 &
```

## 5. 验证服务是否正常

检查后端健康接口：

```bash
curl http://127.0.0.1:8000/health
```

检查监听端口：

```bash
ss -ltnp | rg ':8000|:8501|:7474|:7687|:19530|:9091'
```

查看日志：

```bash
tail -n 100 logs/uvicorn.out
tail -n 100 logs/streamlit.out
```

## 6. 本地浏览器访问

如果你在本地电脑访问远程服务器，建议端口转发：

```bash
ssh -L 8501:127.0.0.1:8501 -L 8000:127.0.0.1:8000 用户@服务器IP
```

本地浏览器打开：

- 前端：`http://127.0.0.1:8501`
- API 文档：`http://127.0.0.1:8000/docs`

## 7. 查看和停止进程

查看进程：

```bash
ps -ef | rg 'uvicorn|streamlit'
```

停止进程：

```bash
kill <PID>
```

如果要停掉基础服务：

```bash
docker compose down
```

## 8. 显存查看

查看显存：

```bash
nvidia-smi
```

简洁查看：

```bash
nvidia-smi --query-gpu=name,memory.total,memory.used,memory.free,utilization.gpu --format=csv
```

## 9. 只有在需要时才重建数据

以下情况才需要重建：

- 更换了新的 PDF 手册
- 删除了 Milvus / Neo4j 数据
- `data/processed/chunks.json` 被重新生成

重建向量索引：

```bash
python scripts/build_index.py --input data/raw/
```

重建知识图谱：

```bash
python scripts/build_graph.py --chunks-file data/processed/chunks.json
```

## 10. 常用排查

后端起不来时先看：

- `logs/uvicorn.out`
- `nvidia-smi`
- `docker ps`

前端打不开时先看：

- `logs/streamlit.out`
- `ss -ltnp | rg ':8501'`
- 是否已做端口转发或安全组放行
